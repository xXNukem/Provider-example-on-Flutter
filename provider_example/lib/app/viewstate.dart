/*Diferent States of your project. The most usual thing is having this two states.
Idle is when your app is doing nothing, and busy is when your app is waiting for something, for example when your app is waiting for the api response. */
enum ViewState { Idle, Busy }
